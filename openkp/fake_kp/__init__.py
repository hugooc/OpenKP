"""fake_kp: a local mock of Kaiser's portal endpoints for dev.

Inspired by Open Record's fake-mychart. Lets us iterate on parsers and MCP
tools without hitting real Kaiser servers (which would eventually get us
rate-limited or flagged).

TODO: wire up once we've mapped Kaiser's actual endpoints.
"""
